# thread-safe-inventory
# Thread-Safe Inventory Monitor 🦀

Sistem pemantauan stok sederhana yang dibangun dengan **Rust** untuk mempelajari konsep *concurrency* dan *memory safety*.

### Fitur Utama:
- **Multi-threading:** Menggunakan `std::thread` untuk simulasi pemrosesan paralel.
- **Thread Safety:** Proteksi data menggunakan `Arc<Mutex<T>>`.
- **Error Handling:** Implementasi `Result` dan `Option` yang aman.
- **Colorful CLI:** Output terminal berwarna menggunakan ANSI escape codes.

## 📸 Preview Aplikasi
